name: Automação Diária Briefing Safety

on:
  schedule:
    # Roda às 14:00 (Brasília) de Segunda a Sexta
    # Terça-feira (1) a Sábado (6), às 17:00 UTC (= 14:00 Brasília em horário de verão)
    - cron: '0 17 * * 1-5'
  
  # Permite disparo manual via GitHub UI
  workflow_dispatch:

jobs:
  automacao-briefing:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      # 1. Faz checkout do código
      - name: Checkout repositório
        uses: actions/checkout@v3
      
      # 2. Configura Python
      - name: Configurar Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      
      # 3. Instala as dependências
      - name: Instalar dependências
        run: pip install -r requirements.txt
      
      # 4. Executa a automação
      - name: Executar automação Briefing Safety
        run: python main.py
        env:
          BRIEFING_NOME: "Max Millian Matheus Pereira de Sales"
          BRIEFING_PLACA: "qux5f90"
          BRIEFING_URL: "https://forms.gle/PdgvP4GyG3UWWZYM9"
      
      # 5. Commit e push do relatório atualizado
      - name: Commit e push do relatório
        run: |
          git config --global user.name "🤖 Robo Briefing Safety"
          git config --global user.email "noreply@github.com"
          git add RELATORIO_SEMANAL.md || true
          git commit -m "📝 Atualizando relatório semanal [skip ci]" || true
          git push